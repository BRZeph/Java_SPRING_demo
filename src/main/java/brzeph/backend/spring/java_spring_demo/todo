TODO:
2:
1) update client operations to allow for CRUD. Already implemented: CRD.
2) update /register and /login methods.


4:
1) implement info/warn messages into the code. do not implement debug messages (Maybe?).
2) implement better exception handling (search the code for generic throws).
2.1) put a better response inside all findAll() end-points when no entries are found.


5:
1) update "new entities attributes.txt" for the changes of permissions/clients/users.
2) update entity/dto attributes to the actual attributes they should have, currently the
attributes are just [PLACEHOLDER]'s. Use "new entities attributes.txt" for reference.
3) update orders entity/rep considering the new differentiation of User/Client.
4) update DB architecture considering new changes.

6:
1) implement validations for CRUD (such as create with a password longer than 6 digits etc).


...6:
1) create views (via transaction(?)).
2) fix this warn:
[restartedMain] WARN  o.s.s.c.a.a.c.InitializeUserDetailsBeanManagerConfigurer$InitializeUserDetailsManagerConfigurer -
Found 3 UserDetailsService beans, with names [clientService, customUserDetailsService, userService]. Global Authentication
Manager will not use a UserDetailsService for username/password login. Consider publishing a single UserDetailsService bean.


TO CONSIDER:
1) consider creating an enum for ServerConfigService.
2) consider creating an enum/constant.class for api paths.